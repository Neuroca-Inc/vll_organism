# Rung 1A Protocol Lock

Experiment ID: `VLL-R1A-HEATMAP-HEATSCOUT-01`

The machine-readable authority for parameters and the decisive rule is `EXPERIMENT_PLAN.json`.

The research burden is intentionally narrow: determine whether HeatMap carries usable local V0 activity information and whether HeatScout can exploit correctly located values under matched bounded work. The experiment explicitly separates mechanism validation from semantic interpretation.

No TrailMap feedback is admitted in this package. No negative semantic result may be used as justification to add FrontierScout or another mechanism inside the same run.
